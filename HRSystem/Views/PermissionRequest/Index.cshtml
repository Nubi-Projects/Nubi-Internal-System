@model IEnumerable<Nubi.Models.PermissionRequest>
@using Microsoft.AspNet.Identity
@{
    ViewBag.Title = "Index";
    Nubi.Models.Mngr mngr = new Nubi.Models.Mngr();
}

<h2>Index</h2>

 

@if (ViewBag.per)
{
    <a href="@Url.Action("Create")" class="btn btn-primary">اضافة</a>
}
else
{
    <a href="@Url.Action("Create")" class="btn btn-primary disabled">اضافة</a>
}



<table class="table table-hover">
    <thead>
        <tr>
            <th>
                Request Date
            </th>
            <th>
                Time From
            </th>
            
            <th>
                Time To
            </th>
            
            <th>
                Permission Date
            </th>
            
            <th>
                Permission Type
            </th>

            <th>
                Note
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Where(e => e.IsDeleted == false).ToList())
            {
            <tr onclick="">
                <td>
                    @Html.DisplayFor(modelItem => item.RequestDate)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.TimeFrom)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.TimeTo)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.PermissionDate)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.PermissionType.Type)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Note)
                </td>

                <td>
                    @Html.ActionLink("تعديل", "Edit", new { id = item.ID }) |

                    @Html.ActionLink("حذف", "Delete", new { id = item.ID })
                </td>
            </tr>
        }
    </tbody>
</table>
